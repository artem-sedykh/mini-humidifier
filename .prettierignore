dist
node_modules
package-lock.json
release_notes

# Markdown is left alone on purpose. The option tables in docs/ have long
# description cells, and prettier pads every row to the width of the widest
# one - 370 columns for docs/configuration.md - which makes the source
# unreadable and every documentation diff enormous.
*.md

# The MkDocs theme override is a Jinja template, not HTML: prettier reformats
# `{% block %}` tags as if they were text and breaks the template.
overrides
